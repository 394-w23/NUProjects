import React from 'react';
import Button from 'react-bootstrap/Button';
import "./AddButton.css";

export default function AddButtonApp() {
    return (
        <Button className="add-button">Add New Position Listing</Button>
    );
}